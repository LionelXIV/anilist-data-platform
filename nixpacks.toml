# Nixpacks : conserver le fournisseur Python et ajouter les libs mysqlclient.
# Sans "...", la liste nixPkgs remplace Python et le build échoue (python: command not found).

[phases.setup]
nixPkgs = ["...", "libmysqlclient", "pkg-config"]
