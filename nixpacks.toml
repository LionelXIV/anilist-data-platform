# Nixpacks : conserver Python et installer les en-têtes MySQL via apt
# (pkg-config mysqlclient / mariadb), requis pour compiler mysqlclient.

[phases.setup]
nixPkgs = ["..."]
aptPkgs = ["...", "default-libmysqlclient-dev", "pkg-config", "build-essential"]
